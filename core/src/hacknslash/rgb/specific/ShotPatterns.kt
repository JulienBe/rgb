package hacknslash.rgb.specific

import hacknslash.rgb.general.gameobjects.GShotPattern

// too much clutter would probably mean too much patterns. Might change
object ShotPatterns {
    val basic = GShotPattern(1f, 0f, Bullet::class)
}