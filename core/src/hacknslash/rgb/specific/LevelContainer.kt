package hacknslash.rgb.specific

import com.badlogic.gdx.Game
import hacknslash.rgb.general.GScreen

class LevelContainer(game: Game) : GScreen(game) {

    override fun render(delta: Float) {
        super.render(delta)
    }
}